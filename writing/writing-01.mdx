---
title: My First Post
date: 2021-12-24
excerpt: Bu  bir excerpt
---

# Başlık 1

Bu bir **örnek** MDX dosyasıdır. Aşağıda farklı içerik türlerine dair örnekler bulabilirsiniz.

## Başlık 2

### Başlık 3

Bir paragrafa örnek:
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nunc vulputate libero et velit interdum, ac aliquet odio mattis.

Bir listeye örnek:

- Liste öğesi 1
- Liste öğesi 2
  - Alt liste öğesi 1
  - Alt liste öğesi 2
- Liste öğesi 3

Numaralı bir listeye örnek:

1. Birinci öğe
2. İkinci öğe
3. Üçüncü öğe

Bir kod bloğuna örnek:

```javascript
function merhabaDunya() {
  console.log("Merhaba Dünya");
}
```

Inline kod örneği: `const foo = 'bar';asdasdasd asda asd asd as das d asd as d asd asd as d`

Bir alıntıya örnek:

> Bu bir alıntı örneğidir.

Bir bağlantıya örnek: [Google](https://www.google.com)

Bir resme örnek:
![Örnek Resim](https://via.placeholder.com/150)

JSX bileşenlerine örnek:

Bir tablonun örneği:

| Başlık 1 | Başlık 2 |
| -------- | -------- |
| Hücre 1  | Hücre 2  |
| Hücre 3  | Hücre 4  |

Bir form örneği:

<form>
  <label htmlFor="name">İsim:</label>
  <input type="text" id="name" name="name" />
  <button type="submit">Gönder</button>
</form>

Daha karmaşık JSX bileşen örneği:

```jsx
import React, { useState } from "react";

function Sayac() {
  const [sayi, setSayi] = useState(0);

  return (
    <div>
      <p>Sayac: {sayi}</p>
      <button onClick={() => setSayi(sayi + 1)}>Arttır</button>
      <button onClick={() => setSayi(sayi - 1)}>Azalt</button>
    </div>
  );
}

<Sayac />;
```

Burada MDX dosyalarında kullanılabilecek temel örneklerin çoğunu bulabilirsiniz. MDX dosyaları hem Markdown hem de JSX'i desteklediği için oldukça esnektir ve güçlüdür.
